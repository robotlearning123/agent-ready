# agent-ready

[![npm version](https://img.shields.io/npm/v/agent-ready.svg)](https://www.npmjs.com/package/agent-ready)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js](https://img.shields.io/badge/node-%3E%3D20.0.0-brightgreen.svg)](https://nodejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.7-blue.svg)](https://www.typescriptlang.org/)

**让每个代码仓库都为 AI Agent 做好准备！**

Factory 兼容的仓库成熟度扫描 CLI 工具，基于 **9 大支柱 / 5 级成熟度** 模型评估仓库，输出可操作的就绪度报告。

## 特性

- **9 大支柱评估**：文档、代码风格、构建、测试、安全、可观测性、环境、CI/CD、单体仓库
- **5 级成熟度**：L1 (基础) → L5 (自治)
- **80% 门控规则**：检查通过率 ≥80% 且所有必需项通过才能达到该级别
- **可扩展配置**：基于 YAML 的检查定义
- **多种输出**：JSON (机器可读) + Markdown (终端显示)
- **初始化命令**：从模板生成缺失文件
- **单体仓库支持**：检测并扫描各个子应用

## 安装

```bash
# 使用 npx (无需安装)
npx agent-ready scan .

# 或全局安装
npm install -g agent-ready
```

## 在线 API

使用托管 API 进行扫描，无需安装：

```bash
# 提交扫描
curl -X POST https://agent-ready.org/api/scan \
  -H "Content-Type: application/json" \
  -d '{"repo_url":"https://github.com/owner/repo","language":"zh"}'

# 查询扫描状态
curl https://agent-ready.org/api/scan/{scan_id}

# 列出可用配置
curl https://agent-ready.org/api/profiles
```

### API 特性

- **多智能体并行分析**：9 个 Agent 同时分析不同支柱
- **中英文报告**：设置 `"language":"zh"` 或 `"language":"en"`
- **快速扫描**：每个仓库约 300ms-2000ms
- **详细报告**：执行摘要、支柱详情、改进路线图

## 快速开始

```bash
# 扫描当前目录
agent-ready scan .

# 查看需要添加哪些文件来提升分数
agent-ready init --dry-run

# 生成缺失文件
agent-ready init --level L2
```

## 使用方法

### 扫描仓库

```bash
# 扫描当前目录
agent-ready scan .

# 扫描指定路径
agent-ready scan /path/to/repo

# 使用指定配置
agent-ready scan --profile factory_compat

# 仅输出 JSON
agent-ready scan --output json

# 详细输出包含所有待办项
agent-ready scan --verbose

# 检查到指定级别
agent-ready scan --level L2
```

### 初始化缺失文件

```bash
# 生成所有缺失的推荐文件
agent-ready init

# 生成 L2 级别需要的文件
agent-ready init --level L2

# 生成特定检查项的模板
agent-ready init --check docs.agents_md

# 预览将要创建的文件
agent-ready init --dry-run
```

## 9 大支柱 / 5 级成熟度模型

### 支柱 (Factory.ai 兼容)

| 支柱 | 说明 |
|------|------|
| **文档** | README, AGENTS.md, CONTRIBUTING, CHANGELOG |
| **代码风格** | EditorConfig, linter, formatter, 类型检查 |
| **构建系统** | 包管理, 构建脚本, 锁文件, CI/CD |
| **测试** | 测试文件, 测试框架配置 |
| **安全** | .gitignore, 密钥模式, CODEOWNERS, Dependabot |
| **可观测性** | 日志, 追踪, 指标框架 |
| **开发环境** | .env.example, devcontainer, docker-compose |
| **任务发现** | Issue 模板, PR 模板 |
| **产品** | 特性开关, 分析, A/B 测试 |

### 级别 (Factory.ai 兼容)

| 级别 | 名称 | 说明 |
|------|------|------|
| L1 | 基础级 | 代码可运行但需要手动设置 |
| L2 | 文档化 | 有文档和部分自动化 |
| L3 | 标准化 | 生产就绪，流程清晰 |
| L4 | 优化级 | 快速反馈循环，全面测试 |
| L5 | 自治级 | 自我改进，精密编排 |

### 级别门控规则

级别**达成**条件：
1. 该级别**所有必需检查**通过
2. 该级别**≥80% 检查**通过
3. **所有前置级别** (L1 到 L(N-1)) 已达成

## 多智能体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         多智能体并行架构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户提交 GitHub URL                                                        │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────┐      │
│   │               🎯 协调者 Agent (Orchestrator)                      │      │
│   │   - 任务分解    - Agent 调度    - 进度监控    - 结果聚合         │      │
│   └─────────────────────────────────────────────────────────────────┘      │
│         │                                                                   │
│         ▼ 并行分发任务                                                       │
│   ┌─────────────────────────────────────────────────────────────────┐      │
│   │   📖文档  ✨风格  🔧构建  🧪测试  🔒安全  📊可观  🌍环境  📋任务  🚀产品   │      │
│   │   (9 个支柱 Agent 并行执行)                                       │      │
│   └─────────────────────────────────────────────────────────────────┘      │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────┐      │
│   │   📝 评估 Agent → 📄 报告 Agent → 最终报告 (中/英文)              │      │
│   └─────────────────────────────────────────────────────────────────┘      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 性能对比

```
顺序执行: 文档→风格→构建→测试→...→产品 ≈ 18秒
并行执行: 9 Agent 同时 ≈ 2秒 (提速 9 倍!)
```

## 项目结构

```
agent-ready/
├── src/                      # CLI 核心代码
│   ├── index.ts              # CLI 入口
│   ├── types.ts              # 类型定义
│   ├── checks/               # 检查实现
│   ├── engine/               # 级别门控逻辑
│   └── utils/                # 工具函数
├── api/                      # 在线 API (多智能体)
│   ├── src/
│   │   ├── index.ts          # Fastify 服务器
│   │   ├── agents/           # 多智能体系统
│   │   │   ├── orchestrator.ts   # 协调者
│   │   │   ├── evaluator.ts      # 评估者
│   │   │   ├── reporter.ts       # 报告生成
│   │   │   └── pillars/          # 9 个支柱 Agent
│   │   ├── routes/           # API 路由
│   │   └── i18n/             # 国际化 (中/英)
│   ├── Dockerfile
│   └── docker-compose.yml
├── profiles/
│   └── factory_compat.yaml   # 默认配置文件
└── templates/                # 初始化模板
```

## 开发

```bash
# 安装依赖
npm install

# 开发模式运行
agent-ready scan

# 类型检查
npm run typecheck

# 运行测试
npm test

# 生产构建
npm run build
```

## 在线服务

- 🌐 前端界面: https://agent-ready.org/
- 🔌 API 接口: https://agent-ready.org/api/
- ❤️ 健康检查: https://agent-ready.org/api/health
- 📋 配置列表: https://agent-ready.org/api/profiles

## 许可证

MIT
