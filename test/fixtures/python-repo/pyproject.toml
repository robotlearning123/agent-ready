[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-test-repo"
version = "1.0.0"

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true
